<h2>Mixes index</h2>

<ul>
<% @mixes.each do |mix| %>
   <li><%= link_to mix.key, mix_path(mix.key) %> - <%= link_to "Delete",  mix_path(mix.key), method: :delete, confirm: 'Are you sure you want to delete ' + mix.key + '?' %></li>
<% end %>
</ul>

<strong><%= link_to "Add New Set", new_mix_path %></strong>